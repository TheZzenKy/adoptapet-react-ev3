ETAPA 1: 
No utilice IA para esta parte

ETAPA 2:
Ahora necesito que:
El componente debe recibir por props: nombre, raza, edad, especie, descripcion,
caracteristicas.
Tambien muestra la información de forma visual y ordenada.
Y aplica un estilo diferenciado según la especie (un color/etiqueta distinto para Perro, Gato,
Otro).
Tambien que me definas los tipos de las props con PropTypes (o documenta claramente cada prop) y
entrega valores por defecto para evitar errores cuando falte un dato.
Renderizes el texto siempre como contenido JSX normal. No uses
dangerouslySetInnerHTML.
Y no dejes credenciales, claves ni datos sensibles escritos en el código.

Esto fue útil ya que se define los datos de entrada y el comportamiento visual, dejando una regla clara.


ETAPA 3:
A continuación necesito que me:
Que ListaMascotas.jsx reciba el array de mascotas por props.
Recorre el array con .map() y renderiza un MascotaCard por cada mascota.
Tabmien asigna correctamente la prop key usando el id de cada elemento (no uses el índice del
array).
Y en App.jsx, renderiza ListaMascotas pasándole los datos importados.

Esto fue útil ya que se definen los array por props asi mismo conectando desde app.jsx. Permitiendole el flujo completo

ETAPA 4:
Quiero que me implementes en:
FiltroEspecia.jsx botones o un selector para filtrar por la especie: “Todas”, “Perro”, “Gato”, “Otro”.
En App.jsx, usa useState para guardar el filtro seleccionado y un campo de búsqueda por nombre.
También aplicame ambos filtros sobre el array antes de pasarlo a ListaMascotas.
Usa un renderizado condicional para mostrar el mensaje “No hay mascotas que coincidan” cuando el resultado quede vacío.
Al igual el campo de búsqueda debe ser un componente controlado (su valor vive en el estado de React).
Y valida y normaliza el texto de búsqueda (recorta espacios y limita el largo) antes de usarlo, para no confiar ciegamente en la entrada del usuario.

Fue útil ya que se define el control, el manejo en caso de algo vacio y una regla para la entrada del usuario evitando conflictos en la busquedad.

ETAPA 5:
Como ultimo necesito que me:
Resaltes visualmente los elementos con adopcionUrgente: true (por ejemplo, una etiqueta “URGENTE” o un borde distinto).
Agregues un contador que muestre cuántas mascotas tienen adopción urgente.
Y me verifiques que toda la app corre sin errores ni warnings en consola.

Útil ya que se agrega una regla visual y se mejora la lectura visual sin cambiar la estructura base